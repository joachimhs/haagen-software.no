<div class="wrap pageContent">
    <div class="row">
        <div class="col-md-12">
            <h2>{{name}}</h2>

            <p class="ingress">{{intro}}</p>

            {{#if largeImg}}
                <img {{bind-attr src=largeImg}} />
            {{/if}}

            {{markdown content.content}}

            {{link-to "Back to products" "products"}}
        </div>
    </div>
</div>

<div class="mediumTopMargin"></div>